// fetch_json is a function that comes from the standard library.
// it is built-in and has an effect of `NetworkRequest`.

fn random(a: number[]) : number {}

fn test() {
    let a = [1, 2, 3]
    let b = random(
        [1,
        2,
    3]
    ) 
}