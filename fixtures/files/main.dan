type Identity<T> = T => T;

fn identity_proxy(identity: Identity) {
    return identity;
}

fn foo(identity: Identity) {
    // Pass the identity function to itself :D
    let a = identity_proxy(identity);
    let b = a("foo");
    // let c = b(1);
    // let f = b("foo");
    return b;
}
