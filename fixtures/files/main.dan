struct ButtonState {
    isPressed: bool
    isHovered: bool
    isFocused: string
}

fn main() {
    let buttonState = ButtonState(
        isPressed: false,
        isHovered: false,
        isFocused: "false" 
    )

    let isActive = buttonState.isHovered || buttonState.isFocused
}