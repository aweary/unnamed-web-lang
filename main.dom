component UserAvatar(user: User, size: AvatarSize) : Node {
  match user.type {
    UserTypes.Admin => <AdminAvatar user={user} size={size} />,
    UserTypes.User => <BaseUserAvatar user={user} size={size} />,
    UserTypes.Guest => <GuestAvatar user={user} size={size} />,
  }
}