function Button(label: string) : ButtonRole {
  return (
    <button class="btn">
      {label}
    </button>
  );
}

function AboutSection(title: string) : Node {
  return (
    <div>
      {title}
      <Button label="Push Me!" />
    </div>
  )
}