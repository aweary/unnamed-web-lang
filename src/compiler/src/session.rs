/// The compile session is like a central database that we use across all stages
/// of parsing. It provides a consistent way for us to manage memory and caching
/// across different data types and refinements.
/// Currently only used for passes after the IR is created.
struct CompileSession {

}